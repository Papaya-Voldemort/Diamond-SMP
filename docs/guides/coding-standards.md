# Coding Standards

- Use Java 21 language level.
- Favor records for simple immutable descriptors.
- Keep platform adapters separated from domain records.
- Prefer small registries and explicit service installers over static globals.
- Document every new feature family in `docs/features`.

