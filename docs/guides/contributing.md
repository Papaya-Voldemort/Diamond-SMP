# Contributing

- Keep gameplay logic out of `plugin-bootstrap`.
- Add feature docs before adding feature code.
- Prefer adding new contracts to `core-api` when multiple modules will consume them.
- Add or update config placeholders alongside new feature surfaces.
- Keep module dependencies one-directional.

